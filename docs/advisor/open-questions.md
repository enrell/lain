# Questions reserved for the user — lain server

The executor adds a row when the advisor returns `needs-user`.
Never implement the item until the Answer column is filled.

| ID | Date | Question | Context and options | User answer | Became decision |
|----|------|----------|---------------------|-------------|-----------------|
| <!-- Example: Q-001 | 2026-09-11 | Add dependency X? | ... | ... | D-014 | --> | | | | | |
| Q-001 | 2026-09-11 | Which machine supplies the Web UI palette? | A: read the Omarchy palette on the Lain server host and share it with every browser. B: do not expose a host palette and retain the static web fallback. Browsers cannot read the local Omarchy file directly. | A — use the server host palette; keep Svelte. | D-020 |
| Q-002 | 2026-09-11 | May the normalized theme endpoint be public? | A: public read-only palette endpoint so setup and login are themed. B: authenticated endpoint, with the static fallback before login. Only normalized colors and mode are returned, never paths or file contents. | A — public normalized palette. | D-020 |
| Q-003 | 2026-09-11 | May a static ten-direction WebUI showroom live under `web/design-showcase/` without entering routes, build, or production? | A: yes, temporary and isolated. B: keep it outside the repository. | A — explicitly requested as ten HTML examples plus an index. | D-021 |
| Q-004 | 2026-09-11 | May the showroom explore alternative navigation models? | A: preserve the current information architecture and vary visual treatment only. B: also explore non-binding navigation models. | B — explore everything as non-binding examples. | D-021 |
| Q-005 | 2026-09-11 | Should the selected Monolith + Nocturne production direction preserve current UX behavior or redefine hero policy, navigation, rail taxonomy/order, or motion? | A: visual-only adaptation preserving all four behaviors. B: explicitly redefine selected behaviors before implementation. | Use clean top navigation; preserve hero policy and all useful rails; use restrained cinematic motion. | D-022 |
| Q-006 | 2026-09-11 | Add an optional `native_title` field (omitempty, additive) to `MetadataRecord`+`Enrichment`, mapping AniList `title.native` / Kitsu `en_jp` / Jikan `title_japanese`, to enable the outlined Japanese title line in the Monolith hero? | A: yes — additive field now, JP line ships this phase. B: no — hero ships without the JP line, contract untouched. The data already exists in providers but is dropped in the overlay; lain-desktop consumes the same shape. | B — hero ships without the JP line, contract untouched. | D-032 |
| Q-007 | 2026-09-12 | May the reserved transcode slice begin now as a server-only narrow slice (ffmpeg-via-exec, one new endpoint/capability, versioned contract)? | A: yes, begin narrow slice now. B: stay reserved, keep honest transcode-required. Trigger: user report "faltou fazer o transcoding"; wiki reserves transcode to its own slice (D-009, taste.md:25). | A — begin narrow slice now. | D-023 |
| Q-008 | 2026-09-12 | May the first transcode slice add to the playback contract (new Plan mode/session, new gateway stream endpoint with Range, `lain.playback.transcode@1`)? | A: yes, additive versioned. B: no, keep v0.1 contract untouched. Depends on Q-007 = A. | A — additive versioned. | D-024 |
| Q-009 | 2026-09-12 | What bounds the first transcode slice? | A: browser MKV→compatible-MP4 only. B: wider matrix from start (state which: HEVC, ASS burn-in, audio). Depends on Q-007 = A. | A — MKV→MP4 only. | D-023 |
| Q-010 | 2026-09-12 | Add TMDB provider now (needs user API key; never in code, only env/operator config)? | A: yes, user supplies the key and states where it lives; executor implements stdlib provider via politeClient. B: no, TVMaze only for now. | B — TVMaze only, no key supplied. | D-025 |
| Q-011 | 2026-09-12 | Add a dedicated IMDb provider? | A: no — coverage via TVMaze+TMDB, IMDb IDs only as externals when available. B: yes anyway (state contracted source: paid AWS API, non-commercial dataset, or other — no unauthorized scraping). IMDb has no free official public API. | A — no scraper; externals only. | D-025 |
| Q-012 | 2026-09-12 | Extend `MetadataRecord`/`Enrichment` contract in this slice? | A: no — providers fill only existing poster/cover/synopsis/genres fields. B: yes (list exact fields + version bump). Precedent: even additive `native_title` was reserved (Q-006). | A — keep existing fields only, no extension. | D-032 |
| Q-013 | 2026-09-12 | Confirm slicing TVMaze-first / TMDB-gated-on-key / IMDb-deferred? | A: yes. B: other order (specify). Depends on Q-010/Q-011/Q-012. | A — TVMaze-only slice (TMDB out per Q-010/B, IMDb out per Q-011/A). | D-025 |
| Q-014 | 2026-09-12 | Log destination for slice 1? | A: stdout only (service/docker/journald owns retention). B: stdout + rotating file under data dir (state rotation policy, retention/size, exact path). | | blocked: file sink (slice 1 ships A) |
| Q-015 | 2026-09-12 | May responses carry additive `X-Request-ID` correlating client errors to server lines without a contract bump? | A: yes, header additive, no body change. B: no, correlate server-side only. | B — correlate server-side only, no header. | D-033 |
| Q-016 | 2026-09-12 | May admin enrich errors gain additive structured `detail` (per-provider causes)? | A: no, keep byte-identical, causes server-log only. B: yes, specify exact additive shape + version handling. | A — keep byte-identical, causes server-log only. | D-033 |
| Q-017 | 2026-09-13 | May the next transcode slice add asynchronous status? | A: add a versioned status endpoint plus additive Plan session/state fields. B: background warm only, without contract change. C: retain synchronous preparation only. | A — add versioned asynchronous sessions and status while retaining `@1`. | D-028 |
| Q-018 | 2026-09-13 | What cache lifecycle should transcodes use? | A: bounded size quota with LRU. B: manual prune only. C: operator-managed path/quota policy. | A — quota + LRU; delegate the safe default and mechanics. | D-029 |
| Q-019 | 2026-09-13 | Which codec and track work follows MKV→MP4? | Stay narrow or order HEVC/HDR, subtitle and audio/quality work. | Delegate the best project-specific engineering order. | D-030 |
| Q-020 | 2026-09-13 | Keep progressive MP4 or start HLS/DASH/ABR, and how should hardware acceleration behave? | Choose delivery protocol and never/opportunistic/explicit hardware policy. | Delegate the best project-specific engineering approach. | D-031 |
| Q-021 | 2026-09-15 | Where do agent QA reports and screenshots live — committed, or local only? | A: committed under `qa/reports/` so findings are reviewable in PRs. B: local only in a gitignored run directory; reports name routes, private media and server files. | "Sempre local" — B. | D-035 |
| Q-022 | 2026-09-15 | Is the fix/verify loop a single pass or a cyclic disagreement resolution? | A: engineer fixes once, human reviews. B: cyclic: the auditor that filed a finding re-tests it in its own session (`--session <id>`), verdict returns to the engineer, repeat until both agree or the round budget is spent. | B — cyclic convergence, with an honest "not converged" when the budget runs out. | D-035 |
| Q-023 | 2026-09-15 | Where does the fleet get model and credentials? | A: contributor choice in `.env` (`LAIN_AGENT_MODEL`), API keys only in OpenCode's auth store, hard fail with hints when unset. B: a committed default model. | A — per-contributor choice, no fallback; keys never in the repo. | D-036 |
| Q-024 | 2026-09-16 | When an audit finding can only be fixed by breaking a frozen contract (`D-032`, `D-033`), how should the fleet dispose of it? | A: mark `needs-decision`, do not implement, and open a `Q-0XX` row with the evidence so the freeze can be revisited. B: mark `wont-fix` locally and leave the contract untouched (reports are gitignored, so the signal disappears). C: escalate only when the same contract is hit by N distinct findings. Advisor recommends A; the executor currently parks the finding in `FINAL.md` of the local run and stops spending rounds on it. | | blocked: promotion of parked findings into this ledger (loop ships parked-and-visible) |
| Q-025 | 2026-09-17 | Legibility floor when the operator palette collapses roles (`ui/F-001`, run `20260917-004455`) — which layer owns the guarantee? | On a host whose Omarchy theme sets `selection == color8` (#584e51), `--color-muted`, `--color-line` and `--color-surface-active` collapse onto one value: neutral badges paint #584e51 text on a #584e51 background (1:1) and all muted text sits at 2.45:1. **A:** server derivation floor in `internal/gateway/theme.go` — derive distinct, contrast-checked values per role (muted ≥ 4.5:1 on background; `line`, `surface-active` and the text they carry never identical). Endpoint shape, host-as-source, public read-only and built-in fallback unchanged, but those roles stop mirroring the host verbatim. **B:** keep the projection verbatim and fix the components — neutral badges/monograms stop pairing two same-valued roles (foreground on surface, border-only pills), accepting that muted labels stay at 2.45:1 unless a separate text clamp is authorized. **C:** treat a collapsing or under-contrast host palette as invalid and serve the built-in palette. Advisor suggests A plus the 4.5:1 text bar; D-020 does not forbid A — the parked `conflicts with: D-020` label is imprecise, the open choice is the remedy. | A — the server derivation floor in `internal/gateway/theme.go` is the guarantee (distinct, contrast-checked values per role; muted clears 4.5:1). | D-037 |
| Q-026 | 2026-09-17 | May the player's preparation overlay show an honest, bounded wait? | First play of a 1080p AV1 source sat on a mute spinner ("Preparing a browser-compatible version…") for the whole preparation; measured healthy but slow: `state=running`, ffmpeg consumed the 738 MB / 30 min source over ~15 min (D-030's conservative H.264/AAC encode, D-031 keeps progressive MP4 — no server change proposed here). **A:** elapsed timer (from the already-public `started_at`) plus copy stating this is the first play preparing a browser-compatible version, that sources like AV1/HEVC can take several minutes, that leaving continues preparation server-side, and an explicit leave/back affordance. Web-only, reversible, no dependency, no contract change; every claim is true. **B:** timer + leave affordance only, keep the current one-liner. **C:** other wording dictated by the user. Advisor suggests A; nothing frozen forbids it, but visible copy is the user's call. | A — elapsed timer from the server timestamps, honest copy about the first-play re-encode and its multi-minute cost, an explicit note that leaving continues preparation server-side, plus a leave/back affordance. | D-038 |
| Q-027 | 2026-09-17 | May the `lain.playback.transcode@2` status carry preparation progress? | The status exposes only `state`, profile, method, cached, has_subtitle, error and timestamps, so the UI can never show real progress. **A:** additive `progress` (0..1, omitempty) on `@2`, no version bump; the client derives elapsed/ETA from deltas (the server never promises an estimate it cannot keep honest). **B:** new versioned `@3` payload with progress (optional eta), `@2` retained. **C:** no contract change; the UI stays indeterminate. Precedent: even additive `native_title` (Q-006→D-032), `X-Request-ID` (Q-015→D-033) and enrich `detail` (Q-016→D-033) were reserved; D-028 fixed the `@2` payload. Advisor suggests A. | A — one additive `progress` (0..1, omitempty) on @2, no version bump; no server-side ETA, the client derives pace from deltas. | D-039 |
| Q-028 | 2026-09-17 | May this work change encode speed/quality or add an AV1 direct-play path? | The measured wait is the frozen profile doing its job: `web-mp4-sdr-v2`, `veryfast`/CRF 23, software-only, whose name is part of the session/cache key. **A:** no — keep `web-mp4-sdr-v2`/`veryfast`; the remedy is the honest UI only (a faster preset buys little on a saturated 4c/8t CPU and costs quality/size permanently). **B:** explicit profile revision (faster preset, renamed profile in the session/cache key, documented quality/size cost). **C:** separate reserved slice for browser-capability direct play (the client reports decoder support; AV1/Opus remux to MP4 + WebVTT sidecar) — the only option that removes the wait for AV1 sources. Combinations allowed. Cites D-030, D-031, CONTRACTS.md:60-104. | C — keep the current profile untouched; the AV1 wait is reserved to a separate browser-capability direct-play slice (client reports decoder support; AV1/Opus remux to MP4 + WebVTT), authorized but needing its own plan before code. | D-040 |
| Q-029 | 2026-09-17 | May preparation start earlier than play? | Today the first authenticated POST happens when the player opens, so the first play always bears the full preparation wait. **A:** no — keep play-triggered. **B:** client-initiated earlier preparation from the details-page CTA (still an explicit authenticated POST; D-028 intact). **C:** also proactive server warm after scan — supersedes D-028's "only an authenticated POST starts work" and needs its own decision. | A — stays play-triggered; no details-page CTA and no proactive warm. | D-041 |
| Q-030 | 2026-09-17 | The user mandate "implement Jellyfin transcode completely, all features, including advanced interface config" reopens items delegated to engineering and then frozen or deferred. Which boundary? | **A:** reopen D-031 and D-030 for this slice — HLS/ABR/HW acceleration plus multiple configurable profiles; hardware still bound by D-031's policy (explicit operator opt-in, capability probing, visible software fallback, never silent switching); D-040 (browser-capability direct play) stays its own reserved plan. **B:** reopen delivery (HLS) only, keep D-030's single frozen profile. **C:** keep delivery and profile frozen; implement only runtime settings + UI over the existing profile. Also state whether per-user playback limits (allow transcoding, max bitrate) and transcode-reason reporting are in scope, and confirm the existing exclusions (live TV, music per D-016; trickplay thumbnails). Advisor: needs-user; the deferral ends only when the user says so. | A — reopen D-031 and D-030: HLS + multi-profile/quality + hardware acceleration (explicit opt-in, capability probe, visible software fallback); per-user limits and transcode reasons included; D-040 stays reserved. Live TV/music (D-016) and trickplay stay out. | D-042 |
| Q-031 | 2026-09-17 | If HLS delivery is in scope (Q-030), which web player client? | **A:** `hls.js` as a web-only dependency (Go stays dependency-free, D-001 untouched; web already carries `bits-ui` + `lucide`). **B:** a minimal in-repo MSE client (no dependency, more owned code; Safari needs its native HLS path). **C:** progressive MP4 only; HLS later. Advisor: needs-user; reversing D-031's delivery and adding a runtime web dependency are user calls per taste.md. | A — hls.js as a web-only dependency; Go adds nothing (D-001 untouched). | D-043 |
| Q-032 | 2026-09-17 | Contract evolution for the new session options and richer status. | **A:** new versioned `lain.playback.transcode@3` retaining `@1`/`@2` unchanged, carrying session options (quality/resolution/bitrate/audio/subtitle/delivery) and richer status (encoder, hardware, fallback, reason). **B:** additive fields on `@2` (specify which). **C:** no contract change (settings only, no per-session options). Advisor: needs-user; precedent (D-024, D-028, D-039) requires an explicit answer. | A — new `lain.playback.transcode@3`, `@1`/`@2` retained unchanged. | D-044 |
| Q-033 | 2026-09-19 | The detail page is per-episode; the user reports it should be the anime/series/movie page with an episode grid. Which read answers "the other files of this title", and what happens when an episode is clicked? | Each episode is its own `CatalogItem`; there is no series entity and no title index (`items`, `items_by_library`). **A:** reuse `GET /api/search?q=<title>` and group client-side — no API change, but the detail page then depends on the search plugin. **B:** add an additive `GET /api/catalog/{id}/episodes`, implemented on the concrete catalog service so `lain.catalog.read@1` stays untouched (the gateway already reads that service for `Page`/`Get`). For the click: play the episode from the grid, open a per-episode page, or open a watch page with the episode list beside the video. taste.md reserves public API changes to the user; D-022/D-030 forbid neither. | B — server read; and clicking an episode opens a playback page with the video centred and the episode list in a right sidebar. | D-056 |

