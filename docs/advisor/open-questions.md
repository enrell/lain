# Questions reserved for the user — lain server

The executor adds a row when the advisor returns `needs-user`.
Never implement the item until the Answer column is filled.

| ID | Date | Question | Context and options | User answer | Became decision |
|----|------|----------|---------------------|-------------|-----------------|
| <!-- Example: Q-001 | 2026-09-11 | Add dependency X? | ... | ... | D-014 | --> | | | | | |
| Q-001 | 2026-09-11 | Which machine supplies the Web UI palette? | A: read the Omarchy palette on the Lain server host and share it with every browser. B: do not expose a host palette and retain the static web fallback. Browsers cannot read the local Omarchy file directly. | A — use the server host palette; keep Svelte. | D-020 |
| Q-002 | 2026-09-11 | May the normalized theme endpoint be public? | A: public read-only palette endpoint so setup and login are themed. B: authenticated endpoint, with the static fallback before login. Only normalized colors and mode are returned, never paths or file contents. | A — public normalized palette. | D-020 |
| Q-003 | 2026-09-11 | May a static ten-direction WebUI showroom live under `web/design-showcase/` without entering routes, build, or production? | A: yes, temporary and isolated. B: keep it outside the repository. | A — explicitly requested as ten HTML examples plus an index. | D-021 |
| Q-004 | 2026-09-11 | May the showroom explore alternative navigation models? | A: preserve the current information architecture and vary visual treatment only. B: also explore non-binding navigation models. | B — explore everything as non-binding examples. | D-021 |
| Q-005 | 2026-09-11 | Should the selected Monolith + Nocturne production direction preserve current UX behavior or redefine hero policy, navigation, rail taxonomy/order, or motion? | A: visual-only adaptation preserving all four behaviors. B: explicitly redefine selected behaviors before implementation. | Use clean top navigation; preserve hero policy and all useful rails; use restrained cinematic motion. | D-022 |
| Q-006 | 2026-09-11 | Add an optional `native_title` field (omitempty, additive) to `MetadataRecord`+`Enrichment`, mapping AniList `title.native` / Kitsu `en_jp` / Jikan `title_japanese`, to enable the outlined Japanese title line in the Monolith hero? | A: yes — additive field now, JP line ships this phase. B: no — hero ships without the JP line, contract untouched. The data already exists in providers but is dropped in the overlay; lain-desktop consumes the same shape. | B — hero ships without the JP line, contract untouched. | D-032 |
| Q-007 | 2026-09-12 | May the reserved transcode slice begin now as a server-only narrow slice (ffmpeg-via-exec, one new endpoint/capability, versioned contract)? | A: yes, begin narrow slice now. B: stay reserved, keep honest transcode-required. Trigger: user report "faltou fazer o transcoding"; wiki reserves transcode to its own slice (D-009, taste.md:25). | A — begin narrow slice now. | D-023 |
| Q-008 | 2026-09-12 | May the first transcode slice add to the playback contract (new Plan mode/session, new gateway stream endpoint with Range, `lain.playback.transcode@1`)? | A: yes, additive versioned. B: no, keep v0.1 contract untouched. Depends on Q-007 = A. | A — additive versioned. | D-024 |
| Q-009 | 2026-09-12 | What bounds the first transcode slice? | A: browser MKV→compatible-MP4 only. B: wider matrix from start (state which: HEVC, ASS burn-in, audio). Depends on Q-007 = A. | A — MKV→MP4 only. | D-023 |
| Q-010 | 2026-09-12 | Add TMDB provider now (needs user API key; never in code, only env/operator config)? | A: yes, user supplies the key and states where it lives; executor implements stdlib provider via politeClient. B: no, TVMaze only for now. | B — TVMaze only, no key supplied. | D-025 |
| Q-011 | 2026-09-12 | Add a dedicated IMDb provider? | A: no — coverage via TVMaze+TMDB, IMDb IDs only as externals when available. B: yes anyway (state contracted source: paid AWS API, non-commercial dataset, or other — no unauthorized scraping). IMDb has no free official public API. | A — no scraper; externals only. | D-025 |
| Q-012 | 2026-09-12 | Extend `MetadataRecord`/`Enrichment` contract in this slice? | A: no — providers fill only existing poster/cover/synopsis/genres fields. B: yes (list exact fields + version bump). Precedent: even additive `native_title` was reserved (Q-006). | A — keep existing fields only, no extension. | D-032 |
| Q-013 | 2026-09-12 | Confirm slicing TVMaze-first / TMDB-gated-on-key / IMDb-deferred? | A: yes. B: other order (specify). Depends on Q-010/Q-011/Q-012. | A — TVMaze-only slice (TMDB out per Q-010/B, IMDb out per Q-011/A). | D-025 |
| Q-014 | 2026-09-12 | Log destination for slice 1? | A: stdout only (service/docker/journald owns retention). B: stdout + rotating file under data dir (state rotation policy, retention/size, exact path). | | blocked: file sink (slice 1 ships A) |
| Q-015 | 2026-09-12 | May responses carry additive `X-Request-ID` correlating client errors to server lines without a contract bump? | A: yes, header additive, no body change. B: no, correlate server-side only. | B — correlate server-side only, no header. | D-033 |
| Q-016 | 2026-09-12 | May admin enrich errors gain additive structured `detail` (per-provider causes)? | A: no, keep byte-identical, causes server-log only. B: yes, specify exact additive shape + version handling. | A — keep byte-identical, causes server-log only. | D-033 |
| Q-017 | 2026-09-13 | May the next transcode slice add asynchronous status? | A: add a versioned status endpoint plus additive Plan session/state fields. B: background warm only, without contract change. C: retain synchronous preparation only. | A — add versioned asynchronous sessions and status while retaining `@1`. | D-028 |
| Q-018 | 2026-09-13 | What cache lifecycle should transcodes use? | A: bounded size quota with LRU. B: manual prune only. C: operator-managed path/quota policy. | A — quota + LRU; delegate the safe default and mechanics. | D-029 |
| Q-019 | 2026-09-13 | Which codec and track work follows MKV→MP4? | Stay narrow or order HEVC/HDR, subtitle and audio/quality work. | Delegate the best project-specific engineering order. | D-030 |
| Q-020 | 2026-09-13 | Keep progressive MP4 or start HLS/DASH/ABR, and how should hardware acceleration behave? | Choose delivery protocol and never/opportunistic/explicit hardware policy. | Delegate the best project-specific engineering approach. | D-031 |
| Q-021 | 2026-09-15 | Where do agent QA reports and screenshots live — committed, or local only? | A: committed under `qa/reports/` so findings are reviewable in PRs. B: local only in a gitignored run directory; reports name routes, private media and server files. | "Sempre local" — B. | D-035 |
| Q-022 | 2026-09-15 | Is the fix/verify loop a single pass or a cyclic disagreement resolution? | A: engineer fixes once, human reviews. B: cyclic: the auditor that filed a finding re-tests it in its own session (`--session <id>`), verdict returns to the engineer, repeat until both agree or the round budget is spent. | B — cyclic convergence, with an honest "not converged" when the budget runs out. | D-035 |
| Q-023 | 2026-09-15 | Where does the fleet get model and credentials? | A: contributor choice in `.env` (`LAIN_AGENT_MODEL`), API keys only in OpenCode's auth store, hard fail with hints when unset. B: a committed default model. | A — per-contributor choice, no fallback; keys never in the repo. | D-036 |
| Q-024 | 2026-09-16 | When an audit finding can only be fixed by breaking a frozen contract (`D-032`, `D-033`), how should the fleet dispose of it? | A: mark `needs-decision`, do not implement, and open a `Q-0XX` row with the evidence so the freeze can be revisited. B: mark `wont-fix` locally and leave the contract untouched (reports are gitignored, so the signal disappears). C: escalate only when the same contract is hit by N distinct findings. Advisor recommends A; the executor currently parks the finding in `FINAL.md` of the local run and stops spending rounds on it. | | blocked: promotion of parked findings into this ledger (loop ships parked-and-visible) |

